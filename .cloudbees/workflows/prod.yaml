apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: Go-re-22
          artifact-id: 2407e6e5-21c0-4a9a-a54b-af358199c991
          target-environment: ecr-qa