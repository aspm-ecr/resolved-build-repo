apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: sv-go-test-repo
          artifact-id: 4a3bd856-352e-4b63-8ba2-07c58636a812
          target-environment: ecr-qa